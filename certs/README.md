# Certificados locales (solo para desarrollo)

Estos certificados son **autofirmados** y sirven solo para pruebas locales.  
**No** subir `key.pem` ni `cert.pem` al repositorio.

## Generar certificados con OpenSSL (Windows / macOS / Linux)

1. Abrir terminal en la carpeta `certs`:
   ```bash
   cd certs
